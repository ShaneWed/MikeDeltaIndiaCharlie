public class Softmax {
    // Use this function on the outputs during calculations to convert outputs to probabilities
    public double[] calculateProbabilities(double[] outputs) {

        return outputs;
    }
}
